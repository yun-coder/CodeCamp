syntax = "proto3";

package flyfish.preview;

message PreviewRequest {
  string file_id = 1;
  string filename = 2;
  repeated string requested_features = 3;
}

service PreviewService {
  rpc Open(PreviewRequest) returns (PreviewRequest);
}
