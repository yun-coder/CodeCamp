/**
 * Comic studio public API surface.
 *
 * Re-exports the ComicBook IR plus a few shared types used by both the
 * server (persistence, validation) and the client (UI).
 */

export * from './comic.js';
