代码价值 = 业务价值 + 技术价值
业务价值 = 用户价值 + 商业价值
技术价值 = 长期效率的提升 - 长期维护的成本

# [高可靠的代码充分考虑了各种异常和边界]
  就像一个笑话说的，程序员编写了一个点餐系统，自测时用它点了米粉、面条、酒水都没有问题,交给用户后，用户点了一个炸弹，程序崩了。


高性能可以简单归结为"多快好省"。
-  多：并发支持好
- 快：响应迅速
- 好：质量高、可靠稳定
- 省：节省资源（网络、CPU、内存、硬盘）


在前端优化中，通常运用三大类方法：减少请求资源数量、降低请求资源大小、提升代码运行效率。
* 减少请求资源数量：浏览器缓存、路由懒加载、资源懒加载、合并打包、雪碧图等
* 降低请求资源大小：gzip压缩、文件压缩（jss、css）、图片压缩
* 提升代码运行效率：使用缓存、计算属性、合适的数据结构和算法、防抖节流、gpu加速等

# 编码内功
    - 1、好的命名是成功的一半
    - 2、小而美
    拆分函数、组件化拆分页面、模块化拆分（将大文件拆分为多个小文件）
    一般一个Vue组件文件，不建议超过300行，其中Template模板部分不建议超过100行，否则就带来很大的阅读负担。
    - 3、清晰的结构
        - 1、使用卫语句 就是提前使用return，防止if嵌套层次太深
        - 2、switch代替多个if else
        - 3、跳成链式调用，使用异步方法改成顺序执行
        - 4、使用管道写法代替循环  （只需要关注过滤条件是什么）filter().map
        - 5、同一个层次对话，（一个函数内部的实现，应该在做同一个层次的事情）
        - 6、明确的参数结构
    4、简洁有力的表达
        - 1、简化bool值返回
        - 2、短路求值 &&
        - 3、三元运算赋值
        - 4、语义化的表达 （比如使用includes）
        - 5、箭头函数替代普通函数
        - 程序员最怕的是不确定性，让一切尽在掌握之中才是最佳做法，不要在背后偷偷做事情。
    - 5、不要玩魔术
        - 1、数据来源在预期之内
        - 2、减少依赖全局数据
        - 3、尽可能的使用纯函数




# 代码解耦与提升正交性
- 耦合是指两个或两个以上的体系或两种运动形式间通过相互作用而彼此影响以至联合起来的现象
    - 1、如果你发现你的代码改了一个地方却莫名影响到了其他地方，就说明你的代码存在严重的耦合了
    - 2、 如无必要，勿增全局变量

正交系统

在几何中两条直线相交之后成直角就说它们是正交的，对于向量而言，这两条线相互独立，向着完全不同的方向发展

正交系统的两个主要收益：提高生产力和降低风险


# 解耦的几种方法
     1、黑盒开发（不需要知道内部的实现细节）
     2、分层 （解耦的本质就是关注点分离）mvc前后端分离-mvvm模式框架
        - 1、并行开发
        - 2、提高复用
     3、依赖倒置
            依赖倒置原则（Dependency Inversion Principle，DIP）是一种设计原则，它强调高层模块不应该依赖于低层模块，而是应该依赖于抽象接口。具体来说，依赖倒置原则要求高层模块应该依赖于抽象接口，而不是具体的实现细节。

        - 1、 上层应用不应该依赖于下层模块，它们都应该依赖于抽象
        - 2、抽象不应该依赖于具体，具体应该依赖于抽象
    4、中间层映射，适配层。减少了依赖部分修改后整个系统的修改次数
    5、生产消费，发布订阅，事件，消息订阅
    6、纯函数
        1、函数返回值只取决于参数，如歌参数不变，返回值就不会变
        2、函数执行过程中没有副作用
    7、策略模式  （map映射配置）
    8、责任链
        前端中一般常见的采用use的方式来进行责任链的创建
    9、慎用全局数据



#  如何写好前端组件
提升组件扩展性可以从三个方面去考虑，如何扩展DOM，如何扩展逻辑以及如何扩展样式，
解决了这三个问题基本就解决了组件的扩展性。

扩展组件 = 扩展DOM + 扩展逻辑 + 扩展样式


# 树立正确的编程原则
    1、最少知识原则 LOD: 一个对象应该尽可能少地了解其他对象地内部结构和实现细节
    2、DRY(Don't Repeat yourself): 避免重复代码，尽量使用函数、类、或模块来封装可复用地代码片段
    3、KISS原则（Keep it simple,stupid）: 保持代码简单易懂，避免过度复杂化，尽量使用简洁地解决方案
    4、YAGNI原则 (You Ain't Gonna Need It): 不要过度设计
    5、SOLID原则：这是一组面向对象设计地原则，
        包括单一职责原则(SRP Single Responsibility Principle)，
        开闭原则(OCP Open-Closed principle),
        里氏替换原则(LSP Liskov Substitution Principle)
        接口隔离原则(ISP Interface Segregation Principle)
        依赖倒置原则(DIP Dependency Inversion Principle),
        旨在提高代码地可维护性、可扩展性和可重用性。
    6、高内聚低耦合： 模块内部地组件之间应该紧密相关，模块之间地依赖应该尽量减少，以提高代码地可维护性和可测试性
    7、测试驱动开发(TDD): 先编写测试用例，再编写代码来满足测试用例，以确保代码地正确性和可靠性




# 编程范式
    1、命令式编程(Imperative Programming): 它通过一系列地命令或语句来描述程序地执行步骤。
    在命令式编程中，程序员需要明确地指定程序地每个细节，包括数据地存储，计算地顺序以及控制流程。

    2、面向过程编程(Procedural Programming)：以过程或函数为基本单位，通过顺序执行一系列操作来解决问题。

    3、面向对象编程(Object-Oriented Programming OOP):将问题分解为对象地集合，每个对象具有方法和属性，通过
    对象之间地交互来解决问题。 核心特点： 封装（Encapsulation）,继承，多态，适合大型应用开发

    4、函数式编程(Functional Programming):将计算机视为函数地求值过程，强调函数地纯粹性和不可变性，避免副作用。
        函数是一等公民
        1、函数可以被赋值给变量
        2、函数可以作为参数传递
        3、函数可以作为返回值返回
        4、函数可以存储在数据结构中
    5、逻辑编程(Logic Programming):基于逻辑推理和规则地编程范式，通过定义事实和规则，让计算机自动推导出答案。
        人工智能，专家系统，自然语言处理

    6、声明式编程(Declarative Programming):描述问题的性质和约束，而不是指定如何解决问题，让计算机自动推导出解决方案。

# 测试驱动开发 TDD

# 策略模式
    1、类似 Array.sort Array.filter的方法  将操作抽象出来交给用户自行实现
    2、当代码中充斥着大量重复的分支判断的时候，可以考虑策略模式

# 装饰器模式
    装饰器模式核心要求是在不修改现有对象（类、函数、组件等）的情况下，扩展对象的行为或功能，
    很明显，这符合我们前面讲过的一个重要的软件设计原则：开闭原则（对扩展开发，对修改关闭）。

# 适配器模式
    想到的可能就是电源适配器，电源适配器的作用是转换电压，将电压转换成我们需要的电压
    适配器模式是一种设计模式，它充当一个中间层，将一个类的接口转换成客户端所期望的另一个接口。
    适配器模式允许不兼容的接口之间进行协同工作，使得客户端能够使用不同接口的对象。

# 外观模式

# 产品篇-产品设计于交互原则
