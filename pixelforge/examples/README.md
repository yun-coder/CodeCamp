# pixelforge examples

End-to-end usage examples. These are intentionally minimal — they just
verify the public API works as advertised.

Phase 1 status: directory only. Real examples land alongside the
corresponding Provider / workflow migrations in Phases 2–5.
